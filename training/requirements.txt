# training/requirements.txt
# Pinned environment for logSguarDian ML pipeline (PLAN.md 0.3)
# Python 3.14.5 on macOS Apple Silicon (arm64 Darwin)
# Regenerate check: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# --- Core ML ---
scikit-learn==1.9.0
numpy==2.4.6
pandas==3.0.3
joblib==1.5.3
scipy==1.17.1

# --- ONNX export/runtime ---
skl2onnx==1.20.0
sklearn-compat==0.1.6
onnx==1.21.0
onnxruntime==1.26.0
ml_dtypes==0.5.4

# --- Data I/O ---
pyarrow==24.0.0
PyYAML==6.0.3

# --- Excluded: imbalanced-learn ---
# SMOTE (cmdi augmentation) and RandomUnderSampler were used in the experiment
# documented in training/SAMPLING_STRATEGY.md and docs/results.md F4.4.
# Result was negative (+0.015 F1, discarded). The experiment code was never
# committed to any notebook or .py file — only the prose/analysis is in .md files.
# To reproduce that specific experiment:
#   pip install imbalanced-learn==0.14.2
# This package is intentionally omitted from the final training path.

# --- Notebooks ---
nbconvert==7.17.1
nbformat==5.10.4
nbclient==0.11.0
jupyter_client==8.9.1
jupyter_core==5.9.1
matplotlib==3.11.0
