# logSguarDian — Repository Status Report (superseded)

**This document described the repository as of 2026-06-14**, when
`packages/core` was an empty skeleton and no model past `rf_v1`/`if_v1` had
been trained. Everything in it — the "what does not exist yet" table, the
66-feature contract, the single-worker critical path — describes a state the
project has long since moved past. Kept for historical reference only; do
not use it to answer "what does logsguardian do today."

For the current state, see instead:

- **Repository layout & data flow:** `docs/architecture.md`
- **Detection/decision logic, threshold history:** `docs/decision-policy.md`
- **Public API:** `docs/api.md`
- **CLI reference:** `docs/commands.md`
- **Known limitations:** `docs/limitations.md`
- **Evaluation results (Config 1/2/3):** `docs/results.md`,
  `docs/vulnerable-app-evaluation/`
